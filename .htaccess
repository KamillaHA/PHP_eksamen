RewriteEngine On 

# 1) Servér eksisterende filer og mapper direkte (CSS, JS, billeder) 
RewriteCond %{REQUEST_FILENAME} -f [OR] 
RewriteCond %{REQUEST_FILENAME} -d 
RewriteRule ^ - [L] 

# 2) Hvis der findes en tilsvarende .php-fil, så brug den 
RewriteCond %{REQUEST_FILENAME}.php -f 
RewriteRule ^([^\.]+)$ $1.php [NC,L] 

# 3) Logout må gerne være direkte (eller route via index)
RewriteRule ^logout/?$ app/views/logout.php [L]

# 4) ALT andet går til index.php (router)
RewriteRule ^ index.php [L]
